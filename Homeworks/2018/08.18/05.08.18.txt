1) Покрыть юнит-тестами с использованием Rule TemporaryFolder, SystemOutRule

2) Покрыть юнит-тестами Calendar с использованием SystemOutRule and PowerMock

3) отдельная ветка + pull request

4) Имеется число переведенное в 2 - ую СС.
Если в числе есть нули между(только!) двумя единицами, то это считается
"gap". Нужно найти максимальный "gap" в числе.

5) 1) Create class MyTestMethod with generic static methodcalcNum (with two parameters: an array T[] and
   variable maxElem of type T) that counts the number of elements in
   an array T[] that are greater than a specified element maxElem.

   2) Create a generic class ArrayWorker with generic array field for working Integer and Double
   Add code to counts the number of elements in each of arrays that are greater
   than a specified element.