1) Покрыть юнит-тестами Calendar с использованием SystemOutRule and PowerMock

2) отдельная ветка + pull request

3) Имеется число переведенное в 2 - ую СС.
Если в числе есть нули между(только!) двумя единицами, то это считается
"gap". Нужно найти максимальный "gap" в числе.

4) Create a generic class ArrayWorker with generic array field for working Integer and Double
   Add code to counts the number of elements in each of arrays that are greater
   than a specified element.